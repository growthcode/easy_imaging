require 'spec_helper'

describe EasyImaging do
  it 'has a version number' do
    expect(EasyImaging::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
